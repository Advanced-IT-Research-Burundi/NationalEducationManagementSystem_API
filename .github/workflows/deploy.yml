on:
  push:
    branches:
      - master
name: 🚀 Deploy Application on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        #server: ftp.cloud209.cloudwebhosting.com ftp.clskycompany.com
        server: ftp.nasoni.bi
        username: advanced@nasoni.bi
        password: jy3#%u6vaWC5c4]=
        server-dir: ./